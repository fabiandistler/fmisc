% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smart_parallel.R
\name{setup_parallel}
\alias{setup_parallel}
\title{Setup parallel backend with automatic configuration}
\usage{
setup_parallel(n_cores = NULL, backend = NULL, verbose = TRUE)
}
\arguments{
\item{n_cores}{Number of cores to use. Default is NULL (auto-detect and use
all but one core to keep system responsive)}

\item{backend}{Force a specific backend. Default is NULL (auto-detect).
Options: "mclapply", "parLapply", "doParallel", "doMC", "furrr",
"foreach", "sequential"}

\item{verbose}{Print setup information. Default is TRUE}
}
\value{
A list with cluster object (if applicable) and backend information:
\item{cluster}{Cluster object (or NULL if not applicable)}
\item{backend}{Character string of selected backend}
\item{n_cores}{Number of cores configured}
\item{info}{Backend detection information}
}
\description{
Setup parallel backend with automatic configuration
}
\examples{
\donttest{
# Auto-detect and setup
setup <- setup_parallel()
stop_parallel(setup)

# Use specific number of cores
setup <- setup_parallel(n_cores = 2)
stop_parallel(setup)

# Force specific backend
setup <- setup_parallel(backend = "doParallel")
stop_parallel(setup)
}
}
\seealso{
\code{\link{detect_parallel_backend}}, \code{\link{stop_parallel}},
\code{\link{smart_parallel_apply}}

Other parallel:
\code{\link{detect_parallel_backend}()},
\code{\link{print_parallel_info}()},
\code{\link{smart_parallel_apply}()},
\code{\link{stop_parallel}()}
}
\concept{parallel}
