% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decorators.R
\name{decorate}
\alias{decorate}
\title{Compose several decorators onto a function}
\usage{
decorate(f, ...)
}
\arguments{
\item{f}{A function to decorate.}

\item{...}{Decorators — each a unary function taking \code{f} and returning
a wrapped function.}
}
\value{
The decorated function.
}
\description{
Applies decorators to \code{f} left-to-right so that
\code{decorate(f, d1, d2)} is equivalent to \code{d2(d1(f))} and reads outside-in.
Each decorator must be a unary function \verb{function(g) -> function}.
For decorators with configuration, wrap them in a closure or use the
native pipe directly (which is the recommended idiom).
}
\examples{
\donttest{
add1 <- function(x) x + 1
g <- decorate(
  add1,
  function(fn) with_retry(fn, max_tries = 2, backoff = 0),
  function(fn) with_timing(fn, .threshold = Inf)
)
g(41)
}
}
\seealso{
Other decorators:
\code{\link[=cache_clear]{cache_clear()}},
\code{\link{fmisc_decorators}},
\code{\link[=is_decorated]{is_decorated()}},
\code{\link[=undecorate]{undecorate()}},
\code{\link[=with_cache]{with_cache()}},
\code{\link[=with_logging]{with_logging()}},
\code{\link[=with_rate_limit]{with_rate_limit()}},
\code{\link[=with_retry]{with_retry()}},
\code{\link[=with_timing]{with_timing()}}
}
\concept{decorators}
