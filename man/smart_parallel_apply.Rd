% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smart_parallel.R
\name{smart_parallel_apply}
\alias{smart_parallel_apply}
\title{Universal parallel apply function}
\usage{
smart_parallel_apply(X, FUN, n_cores = NULL, ..., setup = NULL)
}
\arguments{
\item{X}{A vector or list to iterate over}

\item{FUN}{Function to apply to each element}

\item{n_cores}{Number of cores to use (NULL for auto-detection)}

\item{...}{Additional arguments passed to FUN}

\item{setup}{Optional pre-configured setup object from \code{\link{setup_parallel}}.
If provided, n_cores is ignored. Reusing setup is more efficient for
multiple operations.}
}
\value{
A list of results
}
\description{
Applies a function to elements of a vector/list using the best available
parallel backend automatically. This function automatically detects the best
parallelization method for your system and applies FUN to each element of X.
}
\examples{
# Simple parallel computation
result <- smart_parallel_apply(1:10, function(x) x^2)

\donttest{
# With additional arguments
result <- smart_parallel_apply(1:10, function(x, p) x^p, p = 3)

# Reusing setup for multiple operations (more efficient)
setup <- setup_parallel(n_cores = 2)
result1 <- smart_parallel_apply(1:100, sqrt, setup = setup)
result2 <- smart_parallel_apply(1:100, log, setup = setup)
stop_parallel(setup)
}
}
\seealso{
\code{\link{setup_parallel}}, \code{\link{detect_parallel_backend}}

Other parallel:
\code{\link{detect_parallel_backend}()},
\code{\link{print_parallel_info}()},
\code{\link{setup_parallel}()},
\code{\link{stop_parallel}()}
}
\concept{parallel}
