% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decorators.R
\name{with_logging}
\alias{with_logging}
\title{Log calls, results, and errors of a function}
\usage{
with_logging(
  f,
  ...,
  .logger = NULL,
  .log_args = TRUE,
  .log_result = FALSE,
  .name = NULL
)
}
\arguments{
\item{f}{A function to wrap.}

\item{...}{Reserved for future extension.}

\item{.logger}{Optional user function \verb{function(event, data)}; \code{event}
is one of \code{"call"}, \code{"success"}, \code{"error"}; \code{data} is a list with
at least \code{name}, plus event-specific fields. Default \code{NULL} uses cli.}

\item{.log_args}{Logical. Whether to include a short deparse of the
arguments in the \code{"call"} event. Default \code{TRUE}.}

\item{.log_result}{Logical. Whether to include a short deparse of the
return value in the \code{"success"} event. Default \code{FALSE}.}

\item{.name}{Optional character name to display for the function.
Defaults to the deparsed \code{f} symbol.}
}
\value{
A function that forwards its arguments to \code{f}.
}
\description{
Wraps \code{f} with a lightweight logger emitting \code{"call"}, \code{"success"},
and \code{"error"} events. The default logger uses cli alerts. Errors are
logged and re-raised unchanged.
}
\examples{
\donttest{
g <- with_logging(function(x, y) x + y, .log_result = TRUE)
g(1, 2)
}
}
\seealso{
Other decorators: 
\code{\link{cache_clear}()},
\code{\link{decorate}()},
\code{\link{fmisc_decorators}},
\code{\link{is_decorated}()},
\code{\link{undecorate}()},
\code{\link{with_cache}()},
\code{\link{with_rate_limit}()},
\code{\link{with_retry}()},
\code{\link{with_timing}()}
}
\concept{decorators}
