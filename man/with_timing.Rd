% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decorators.R
\name{with_timing}
\alias{with_timing}
\title{Measure a function's elapsed wall-clock time}
\usage{
with_timing(
  f,
  ...,
  .report = c("message", "attribute", "callback"),
  .callback = NULL,
  .threshold = 0
)
}
\arguments{
\item{f}{A function to wrap.}

\item{...}{Reserved for future extension.}

\item{.report}{One of \code{"message"} (default; cli info alert),
\code{"attribute"} (attach \code{attr(result, "elapsed")}), \code{"callback"}
(invoke \code{.callback(name, elapsed, ok)}).}

\item{.callback}{Function used when \code{.report = "callback"}.}

\item{.threshold}{Non-negative numeric. Only report when elapsed time
in seconds is \verb{>= .threshold}. Default \code{0}.}
}
\value{
A function that forwards its arguments to \code{f}.
}
\description{
Wraps \code{f} so each call is timed with \code{proc.time()}. Reports elapsed
time on both success and failure (via \code{on.exit()}), either as a cli
message, attached as an \code{"elapsed"} attribute on the return value, or
passed to a user callback.
}
\examples{
\donttest{
g <- with_timing(function(x) {
  Sys.sleep(0.05)
  x
}, .threshold = 0)
g(42)
}
}
\seealso{
Other decorators:
\code{\link[=cache_clear]{cache_clear()}},
\code{\link[=decorate]{decorate()}},
\code{\link{fmisc_decorators}},
\code{\link[=is_decorated]{is_decorated()}},
\code{\link[=undecorate]{undecorate()}},
\code{\link[=with_cache]{with_cache()}},
\code{\link[=with_logging]{with_logging()}},
\code{\link[=with_rate_limit]{with_rate_limit()}},
\code{\link[=with_retry]{with_retry()}}
}
\concept{decorators}
