% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decorators.R
\name{with_retry}
\alias{with_retry}
\title{Retry a function with exponential backoff}
\usage{
with_retry(
  f,
  max_tries = 3L,
  backoff = 2,
  ...,
  .on_error = NULL,
  .max_delay = Inf,
  .jitter = TRUE,
  .on_retry = NULL
)
}
\arguments{
\item{f}{A function to wrap.}

\item{max_tries}{Positive integer. Maximum number of attempts (including
the first). Default \code{3}.}

\item{backoff}{Non-negative number. Base of the exponential delay in
seconds; delay for attempt \code{k} is \code{backoff^k}. Default \code{2} (i.e. 2, 4,
8, 16 s). Use \code{0} to disable delays.}

\item{...}{Passed to the wrapped function on each call. Reserved for
future extensions; currently forwarded via the wrapper's \code{...}.}

\item{.on_error}{Optional predicate \verb{function(cnd) -> logical}. When
\code{TRUE} the error is retried; when \code{FALSE} it is re-raised immediately.
Default \code{NULL} retries all errors.}

\item{.max_delay}{Numeric cap on individual retry delays in seconds.
Default \code{Inf}.}

\item{.jitter}{Logical. If \code{TRUE} (default), each delay is multiplied by
a uniform random factor in \verb{[0.5, 1.5]} to avoid synchronized retries.}

\item{.on_retry}{Optional callback \verb{function(attempt, delay, cnd)}
invoked before each sleep. Errors in the callback are swallowed.}
}
\value{
A function that forwards its arguments to \code{f}. On persistent
failure raises a condition of class \code{"fmisc_retry_exhausted"}
preserving the original condition's classes.
}
\description{
Wraps \code{f} so that on error the call is retried up to \code{max_tries} times,
with a delay of \code{backoff^attempt} seconds between attempts (optionally
jittered and capped).
}
\examples{
\dontrun{
flaky <- function(x) {
  if (runif(1) < 0.7) stop("transient")
  x * 2
}
robust <- with_retry(flaky, max_tries = 5, backoff = 0.1)
robust(21)
}
}
\seealso{
Other decorators:
\code{\link[=cache_clear]{cache_clear()}},
\code{\link[=decorate]{decorate()}},
\code{\link{fmisc_decorators}},
\code{\link[=is_decorated]{is_decorated()}},
\code{\link[=print.fmisc_decorated]{print.fmisc_decorated()}},
\code{\link[=undecorate]{undecorate()}},
\code{\link[=with_cache]{with_cache()}},
\code{\link[=with_logging]{with_logging()}},
\code{\link[=with_rate_limit]{with_rate_limit()}},
\code{\link[=with_timing]{with_timing()}}
}
\concept{decorators}
