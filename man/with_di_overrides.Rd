% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/di_container.R
\name{with_di_overrides}
\alias{with_di_overrides}
\title{Temporarily override services in a DI container}
\usage{
with_di_overrides(container, overrides, code)
}
\arguments{
\item{container}{An \code{fmisc_di_container} produced by \code{\link[=di_container]{di_container()}}.}

\item{overrides}{A named list mapping service names to override
providers (values or factory functions).}

\item{code}{An expression to evaluate with overrides active. Evaluated
lazily in the caller's environment.}
}
\value{
The value of \code{code}, invisibly if the code itself is invisible.
}
\description{
Applies a set of named overrides to \code{container}, evaluates \code{code},
and restores the prior registrations on exit — including on error.
}
\examples{
\donttest{
con <- di_container()
con$register("db", list(query = function(q) "real"))
with_di_overrides(con, list(db = list(query = function(q) "mock")), {
  con$resolve("db")$query("SELECT 1")
})
con$resolve("db")$query("SELECT 1")
}
}
\seealso{
\code{\link[=di_container]{di_container()}}

Other di: 
\code{\link{di_container}()},
\code{\link{fmisc_di}},
\code{\link{print.fmisc_di_container}()}
}
\concept{di}
