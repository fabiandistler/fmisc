% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/di_container.R
\name{di_container}
\alias{di_container}
\title{Create a dependency-injection container}
\usage{
di_container(parent = NULL)
}
\arguments{
\item{parent}{Optional parent container. When set, unresolved names
fall through to the parent (used by \link[=fmisc_di]{child()}).}
}
\value{
An object of class \code{"fmisc_di_container"} — a list of
closures with the following elements:
\describe{
\item{\code{register(name, provider, ..., .lifecycle = NULL, .deps = NULL)}}{
Register a service. \code{provider} is either a value or a factory
function. \code{.lifecycle} is one of \code{"singleton"} (factory called
once, result cached; default when provider is a function),
\code{"transient"} (factory called every resolve), or \code{"value"}
(provider stored as-is; default when provider is not a
function). \code{.deps} optionally maps factory formal names to
registered service names (defaults to formal-name auto-wiring).}
\item{\code{resolve(name)}}{Resolve a service by name.}
\item{\code{has(name)}}{Test whether a service is registered
(locally or in a parent scope).}
\item{\code{names()}}{Character vector of registered service names.}
\item{\code{unregister(name)}}{Remove a local registration and its
cached singleton.}
\item{\code{override(name, provider, ..., .lifecycle = NULL, .deps = NULL)}}{
Replace an existing registration and return an invisible
\code{restore()} closure that reinstates the prior registration and
clears any cached singleton. See also \code{\link[=with_di_overrides]{with_di_overrides()}}.}
\item{\code{child()}}{Create a new container whose lookups fall
through to this one; local registrations shadow the parent
without mutating it.}
\item{\code{inject(fn, ...)}}{Return a wrapper of \code{fn} whose formals
matching registered service names are pre-bound at call time.
Named \code{...} arguments explicitly pin dependencies.}
}
Factories may declare a formal named \code{.container} to receive the
owning container.
}
\description{
Create a dependency-injection container
}
\examples{
\donttest{
con <- di_container()
con$register("greeting", "hello")
con$register("service", function(greeting) function(who) {
  paste(greeting, who)
})
svc <- con$resolve("service")
svc("world")
}
}
\seealso{
\code{\link[=with_di_overrides]{with_di_overrides()}}

Other di: 
\code{\link{fmisc_di}},
\code{\link{print.fmisc_di_container}()},
\code{\link{with_di_overrides}()}
}
\concept{di}
