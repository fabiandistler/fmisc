# Monitor RAM During Processing

Monitors RAM usage and returns true if threshold is exceeded.

## Usage

``` r
ram_threshold_exceeded(max_ram_mb)
```

## Arguments

- max_ram_mb:

  Maximum RAM threshold in MB

## Value

Boolean indicating if RAM threshold is exceeded
